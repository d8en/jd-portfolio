import { JdViewWrapper } from '../../components/jdViewWrapper/jdViewWrapper';

export function JdHomeView(): React.JSX.Element {
    return (
        <JdViewWrapper>
            <h1>Jeff Dayton</h1>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
            <p>We decided to keep two separate repos: one for front end, one for backend. This was our general stack:</p>
        </JdViewWrapper>
    )
}